{
  "name": "@cohost/services",
  "version": "0.0.1",
  "private": true,
  "exports": {
    "./http-client": "./src/http-client.ts"
  },
  "scripts": {
    "format": "biome format --write ./src",
    "lint": "biome lint ./src",
    "check": "biome check --apply ./src",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@cohost/typescript-config": "workspace:*"
  }
}
